# 🔧 Быстрое исправление ошибки подключения к БД

## Проблема
Ошибка: `Can't reach database server at 'localhost:5432'`

## ✅ Решение: Использовать облачную БД (2 минуты)

### Шаг 1: Создайте бесплатную БД на Neon.tech

1. Откройте https://neon.tech
2. Нажмите **"Sign Up"** (можно через GitHub)
3. Создайте новый проект:
   - Project name: `fk-norm`
   - Region: выберите ближайший (например, `Europe (Frankfurt)`)
   - Database: `fk_norm`
4. После создания скопируйте **Connection string** (выглядит как `postgresql://user:password@host/dbname`)

### Шаг 2: Обновите файл `.env`

Откройте файл `.env` в корне проекта и замените `DATABASE_URL` на скопированную строку:

```env
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
JWT_SECRET="ваша-случайная-строка-для-безопасности-минимум-32-символа"
NODE_ENV="development"
```

### Шаг 3: Примените миграции

```bash
npx prisma migrate deploy
```

### Шаг 4: Создайте администратора

```bash
npm run create-admin admin@example.com admin123
```

### Шаг 5: Перезапустите сервер

Остановите текущий сервер (Ctrl+C) и запустите заново:

```bash
npm run dev
```

## ✅ Готово!

Теперь откройте http://localhost:3000 и войдите:
- Email: `admin@example.com`
- Пароль: `admin123`

---

## Альтернатива: Локальный PostgreSQL

Если хотите использовать локальный PostgreSQL:

1. Установите PostgreSQL: https://www.postgresql.org/download/windows/
2. Запустите PostgreSQL сервис
3. Создайте базу данных:
   ```sql
   CREATE DATABASE fk_norm;
   ```
4. Обновите `.env`:
   ```env
   DATABASE_URL="postgresql://postgres:ваш_пароль@localhost:5432/fk_norm?schema=public"
   ```
5. Выполните шаги 3-5 из решения выше




