# Быстрая настройка базы данных

## 🚀 Вариант 1: Neon.tech (рекомендуется, 2 минуты)

1. Перейдите на https://neon.tech
2. Нажмите "Sign Up" (можно через GitHub)
3. Создайте новый проект:
   - Project name: `fk-norm` (или любое имя)
   - Region: выберите ближайший
   - Database: `fk_norm` (или оставьте по умолчанию)
4. После создания проекта скопируйте **Connection string** (выглядит как `postgresql://user:password@host/dbname`)
5. Откройте файл `.env` в корне проекта и вставьте скопированную строку:
   ```
   DATABASE_URL="postgresql://user:password@host/dbname"
   JWT_SECRET="ваша-случайная-строка-для-безопасности-минимум-32-символа"
   NODE_ENV="development"
   ```
6. Выполните миграции:
   ```bash
   npm run prisma:migrate
   ```
7. Создайте администратора:
   ```bash
   npm run create-admin admin@example.com admin123
   ```
8. Готово! Войдите в систему:
   - Email: `admin@example.com`
   - Пароль: `admin123`

---

## Вариант 2: Supabase (альтернатива)

1. Перейдите на https://supabase.com
2. Создайте аккаунт и новый проект
3. В настройках проекта найдите "Database" → "Connection string"
4. Скопируйте строку подключения
5. Вставьте в `.env` файл
6. Выполните шаги 6-8 из варианта 1

---

## Вариант 3: Локальный PostgreSQL

Если у вас установлен PostgreSQL:

1. Создайте базу данных:
   ```sql
   CREATE DATABASE fk_norm;
   ```

2. В файле `.env` укажите:
   ```
   DATABASE_URL="postgresql://postgres:ваш_пароль@localhost:5432/fk_norm?schema=public"
   JWT_SECRET="ваша-случайная-строка-для-безопасности"
   NODE_ENV="development"
   ```

3. Выполните шаги 6-8 из варианта 1

---

## После настройки БД

Обновите страницу в браузере и попробуйте войти снова!




